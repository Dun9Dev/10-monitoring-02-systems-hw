# Домашнее задание к занятию "13.Системы мониторинга" - Выолнил Shestovskikh Daniil

---
# Обязательные задания

1. Вас пригласили настроить мониторинг на проект. На онбординге вам рассказали, что проект представляет из себя 
платформу для вычислений с выдачей текстовых отчетов, которые сохраняются на диск. Взаимодействие с платформой 
осуществляется по протоколу http. Также вам отметили, что вычисления загружают ЦПУ. Какой минимальный набор метрик вы
выведите в мониторинг и почему?
**Минимальный набор метрик:**
- **CPU:** `cpu_usage_total`, `load_average` — чтобы отслеживать загрузку вычислительных мощностей.
- **Диск:** `disk_used_percent`, `disk_inodes_free` — контроль свободного места и количества inodes (чтобы файлы создавались).
- **Доступность:** `http_up`, `http_response_time`, `http_status_code` — мониторинг доступности платформы и качества ответов.
- **Процесс:** `process_up` — проверка, жив ли основной процесс.

**Почему:** Эти метрики покрывают 4 золотых сигнала SRE (задержка, трафик, ошибки, насыщение).

#
2. Менеджер продукта посмотрев на ваши метрики сказал, что ему непонятно что такое RAM/inodes/CPUla. Также он сказал, 
что хочет понимать, насколько мы выполняем свои обязанности перед клиентами и какое качество обслуживания. Что вы 
можете ему предложить?

**Перевод на бизнес-язык через SLO:**
- Доступность сайта — **99.5%**
- Время ответа — **95% запросов быстрее 2 секунд**
- Успешность формирования отчетов — **99%**
- Свободное место на диске — **не менее 20%**

Это позволяет менеджеру понимать качество обслуживания клиентов без погружения в технические детали.


#
3. Вашей DevOps команде в этом году не выделили финансирование на построение системы сбора логов. Разработчики в свою 
очередь хотят видеть все ошибки, которые выдают их приложения. Какое решение вы можете предпринять в этой ситуации, 
чтобы разработчики получали ошибки приложения?

**Решение для сбора ошибок без бюджета:**
Использовать **Sentry** — бесплатный/opensource инструмент для сбора и анализа ошибок.  
Разработчики добавляют SDK в код, и все исключения автоматически попадают в Sentry с контекстом (стектрейс, окружение).  
Это не требует инфраструктуры и даёт именно то, что нужно — ошибки приложений.
#
4. Вы, как опытный SRE, сделали мониторинг, куда вывели отображения выполнения SLA=99% по http кодам ответов. 
Вычисляете этот параметр по следующей формуле: summ_2xx_requests/summ_all_requests. Данный параметр не поднимается выше 
70%, но при этом в вашей системе нет кодов ответа 5xx и 4xx. Где у вас ошибка?

**Ошибка в расчёте SLA:**
Формула `summ_2xx_requests / summ_all_requests` не учитывает **3xx (редиректы)**, которые также являются успешными ответами.  
Правильная формула:
```
SLA = (summ_2xx_requests + summ_3xx_requests) / summ_all_requests
```
(см. слайд 27 презентации).

---

#
5. Опишите основные плюсы и минусы pull и push систем мониторинга.

**Плюсы и минусы push/pull моделей:**

| Модель | Плюсы | Минусы |
|--------|-------|--------|
| **Push** | + Простая репликация данных<br>+ Гибкая настройка отправки<br>+ Меньше нагрузка на сеть (UDP) | - Сложнее контроль подлинности<br>- Потеря данных при проблемах сети<br>- Труднее отладка |
| **Pull** | + Легче контроль подлинности<br>+ Централизованное управление<br>+ Упрощённая отладка | - Нужно знать адреса агентов<br>- Сложнее с динамическими средами<br>- Одна точка отказа |

#
6. Какие из ниже перечисленных систем относятся к push модели, а какие к pull? А может есть гибридные?

    - Prometheus 
    - TICK
    - Zabbix
    - VictoriaMetrics
    - Nagios
  

**Классификация систем мониторинга:**

| Система | Модель |
|---------|--------|
| Prometheus | Pull (с Pushgateway) |
| TICK | Push |
| Zabbix | Гибридная (push + pull) |
| VictoriaMetrics | Гибридная |
| Nagios | Pull |

#
7. Склонируйте себе [репозиторий](https://github.com/influxdata/sandbox/tree/master) и запустите TICK-стэк, 
используя технологии docker и docker-compose.

В виде решения на это упражнение приведите скриншот веб-интерфейса ПО chronograf (`http://localhost:8888`). 

P.S.: если при запуске некоторые контейнеры будут падать с ошибкой - проставьте им режим `Z`, например
`./data:/var/lib:Z`

**Запуск TICK-стека:**
- Склонирован репозиторий `influxdata/sandbox`
- Запущен через `./sandbox up`
- Chronograf доступен по адресу: `http://192.168.0.243:8888`

![img](https://github.com/Dun9Dev/10-monitoring-02-systems-hw/blob/main/img/Screenshot_20260304_095007.png)

#
8. Перейдите в веб-интерфейс Chronograf (http://localhost:8888) и откройте вкладку Data explorer.
        
    - Нажмите на кнопку Add a query
    - Изучите вывод интерфейса и выберите БД telegraf.autogen
    - В `measurments` выберите cpu->host->telegraf-getting-started, а в `fields` выберите usage_system. Внизу появится график утилизации cpu.
    - Вверху вы можете увидеть запрос, аналогичный SQL-синтаксису. Поэкспериментируйте с запросом, попробуйте изменить группировку и интервал наблюдений.

Для выполнения задания приведите скриншот с отображением метрик утилизации cpu из веб-интерфейса.


**График утилизации CPU:**
- В Chronograf выбран **Explore**
- База: `telegraf.autogen`
- Measurement: `cpu`
- Field: `usage_system`
- График построен, данные поступают

![img](https://github.com/Dun9Dev/10-monitoring-02-systems-hw/blob/main/img/Screenshot_20260304_104419.png)
#
9. Изучите список [telegraf inputs](https://github.com/influxdata/telegraf/tree/master/plugins/inputs). 
Добавьте в конфигурацию telegraf следующий плагин - [docker](https://github.com/influxdata/telegraf/tree/master/plugins/inputs/docker):
```
[[inputs.docker]]
  endpoint = "unix:///var/run/docker.sock"
```

Дополнительно вам может потребоваться донастройка контейнера telegraf в `docker-compose.yml` дополнительного volume и 
режима privileged:
```
  telegraf:
    image: telegraf:1.4.0
    privileged: true
    volumes:
      - ./etc/telegraf.conf:/etc/telegraf/telegraf.conf:Z
      - /var/run/docker.sock:/var/run/docker.sock:Z
    links:
      - influxdb
    ports:
      - "8092:8092/udp"
      - "8094:8094"
      - "8125:8125/udp"
```

После настройке перезапустите telegraf, обновите веб интерфейс и приведите скриншотом список `measurments` в 
веб-интерфейсе базы telegraf.autogen . Там должны появиться метрики, связанные с docker.

Факультативно можете изучить какие метрики собирает telegraf после выполнения данного задания.


**Подключение плагина Docker в Telegraf:**
- В конфиг `telegraf.conf` добавлена секция:
  ```ini
  [[inputs.docker]]
    endpoint = "unix:///var/run/docker.sock"
  ```
- В `docker-compose.yml` добавлен volume `/var/run/docker.sock`
- При запуске Telegraf версии 1.4.0 возникает паника:
  ```
  panic: runtime error: slice bounds out of range
  ```
- **Вывод:** Плагин `docker` несовместим с версией Telegraf 1.4.0 и актуальным Docker API. Для сохранения работоспособности стека плагин отключён. Метрики CPU и system собираются корректно.

![img](https://github.com/Dun9Dev/10-monitoring-02-systems-hw/blob/main/img/Screenshot_20260304_122824.png)

